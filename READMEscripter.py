"""A bash script to read a .sh, .py or .R file and write the
    documentation into the README.txt file for that week"""

import sys

week = argv[1]
file = argv[2]

r = open('/week/file', 'r')

for lines in r:
    for lines in range(10):
        if 

